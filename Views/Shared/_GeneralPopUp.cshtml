<div id="general-popup" class="gp-backdrop" hidden>
    <div class="gp-dialog">
        <button class="gp-close" type="button" aria-label="Close">&times;</button>
        <p id="gp-text" class="mb-3"></p>
        <div class="gp-actions">
            <button id="gp-ok" type="button" class="btn btn-danger">OK</button>
            <button id="gp-cancel" type="button" class="btn btn-secondary">Cancel</button>
        </div>
    </div>
</div>

<style>
    .gp-backdrop {
        position: fixed;
        inset: 0;
        background: rgba(0,0,0,.4);
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 1050;
    }
        .gp-backdrop[hidden] {
            display: none;
        }

    .gp-dialog {
        position: relative;
        background: #fff;
        border-radius: 10px;
        padding: 18px;
        min-width: 320px;
        max-width: 90%;
        box-shadow: 0 12px 32px rgba(0,0,0,.25);
    }

    .gp-close {
        position: absolute;
        right: 8px;
        top: 6px;
        background: transparent;
        border: 0;
        font-size: 28px;
        line-height: 1;
        cursor: pointer;
    }

    .gp-actions {
        display: flex;
        gap: .5rem;
        justify-content: flex-end;
    }
</style>
